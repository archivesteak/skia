diff --git a/src/partition_alloc/BUILD.gn b/src/partition_alloc/BUILD.gn
index 60f0906..531839f 100644
--- a/src/partition_alloc/BUILD.gn
+++ b/src/partition_alloc/BUILD.gn
@@ -967,7 +967,7 @@ if (is_clang_or_gcc) {
         "shim/allocator_shim_alloc_token_symbols.h",
       ]
     }
-    if (is_win) {
+    if (is_win && !is_mingw) {
       shim_headers += [
         "shim/allocator_shim_override_ucrt_symbols_win.h",
         "shim/winheap_stubs_win.h",
@@ -1007,7 +1007,7 @@ if (is_clang_or_gcc) {
       if (is_chromeos || is_linux) {
         shim_sources += [ "shim/allocator_shim_default_dispatch_to_glibc.cc" ]
       }
-      if (is_win) {
+      if (is_win && !is_mingw) {
         shim_sources += [ "shim/allocator_shim_default_dispatch_to_winheap.cc" ]
       }
     }
diff --git a/src/partition_alloc/partition_alloc_base/export_template.h b/src/partition_alloc/partition_alloc_base/export_template.h
index 14dd5dd..19c6b45 100644
--- a/src/partition_alloc/partition_alloc_base/export_template.h
+++ b/src/partition_alloc/partition_alloc_base/export_template.h
@@ -178,8 +178,12 @@
 
 PA_EXPORT_TEMPLATE_TEST(DEFAULT, );  // NOLINT
 PA_EXPORT_TEMPLATE_TEST(DEFAULT, __attribute__((visibility("default"))));
+// Under MinGW, clang predefines __declspec(a) as __attribute__((a)), so the
+// declspec forms collapse into the attribute (DEFAULT) form tested above.
+#if !defined(__MINGW32__)
 PA_EXPORT_TEMPLATE_TEST(EXPORT_DLLEXPORT, __declspec(dllexport));
 PA_EXPORT_TEMPLATE_TEST(DEFAULT, __declspec(dllimport));
+#endif
 
 #undef PA_EXPORT_TEMPLATE_TEST
 #undef PA_EXPORT_TEMPLATE_TEST_DEFAULT_DEFAULT
